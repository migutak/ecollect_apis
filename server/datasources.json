{
  "oracle": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_DATABASE}",
    "password": "${DB_PASSWORD}",
    "name": "oracle",
    "tns": "",
    "user": "ecol",
    "connector": "oracle"
  },
  "mysql": {
    "host": "${MYSQL_HOST}",
    "port": "${MYSQL_PORT}",
    "url": "",
    "database": "${MYSQL_DATABASE}",
    "password": "${MYSQL_PASSWORD}",
    "name": "mysql",
    "user": "${MYSQL_USER}",
    "connector": "mysql"
  }
}
