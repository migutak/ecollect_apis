{
  "oracle": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_DATABASE}",
    "password": "${DB_PASSWORD}",
    "name": "oracle",
    "tns": "",
    "user": "${DB_USER}",
    "connector": "oracle"
  }
}
